# UI migration notes

The rendering, intro/game-over overlays, and sprite editor still live in `src/game.js`.

Next extraction targets:
- render pipeline
- HUD / shop / weapon bar rendering
- intro gate + comic flow
- sprite editor overlay
